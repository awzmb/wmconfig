# Stop apps (browsers/WebRTC/Discord/etc.) from auto-adjusting the mic volume.
# Empty match = all clients; block-source-volume is a stock pipewire-pulse quirk.
pulse.rules = [
    {
        matches = [ {} ]
        actions = { quirks = [ block-source-volume ] }
    }
]

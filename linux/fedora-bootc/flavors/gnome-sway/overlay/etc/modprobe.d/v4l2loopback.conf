# OBS virtual camera loopback device. exclusive_caps=1 makes Chromium/Firefox
# and other apps recognise it as a capture device (without it browsers ignore
# the node); video_nr pins it to /dev/video10 so the label stays stable.
options v4l2loopback devices=1 video_nr=10 card_label="OBS Virtual Camera" exclusive_caps=1

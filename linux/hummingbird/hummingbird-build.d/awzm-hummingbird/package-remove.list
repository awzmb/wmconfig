# Packages to remove from the image after installation.
# Mirrors `pacman -Rdd --noconfirm chromium` from awzmlinux post_install.sh.
# Removed with `dnf remove` in the post_install extension. Ignored if absent.

# chromium

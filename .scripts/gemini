#!/bin/bash
podman run --rm -it --interactive \
  --volume ~/.config/gemini:/root/.gemini:Z \
  --volume "${PWD}":/workspace \
  --env GOOGLE_API_KEY=$(pass private/vertex-api-key) \
  --workdir /workspace \
  localhost/gemini-cli:latest
